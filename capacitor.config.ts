import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dgw.dgwmobile',
  appName: 'DGW',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
